using UnityEngine;
using System.Collections;
using System.IO;

public class DiskWriter {

    public void WriteToDisk(WormList WormToSave)
    {
        /*using(var writer = new BinaryWriter(File.OpenWrite("jee.dat"))){
            writer.Write(WormToSave);
            writer.Close();
        }*/
    }

    void ReadFromDisk()
    {

    }
}
